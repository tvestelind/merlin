sudo: required

language: c

service:
  - docker

addons:
  apt:
    packages:
    - autoconf
    - automake
    - check
    - gcc
    - gperf
    - libdbi-dev
    - libglib2.0-dev
    - libtool
    - unzip
    - wget

before_script:
  - wget https://github.com/naemon/naemon-core/archive/master.zip
  - unzip master.zip
  - pushd naemon-core-master && autoreconf --install --force && ./configure && make && sudo make install && popd
  - autoreconf --install --force

script:
  - ./configure && make && make features/binlog.feature
